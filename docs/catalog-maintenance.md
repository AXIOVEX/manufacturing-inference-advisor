# Catalog maintenance

Policies and neutral component classes live in `catalog/`. Review changes through version control, cite an authoritative source and observation date for factual additions, and retain unknowns when evidence expires. Run the unit suite and a representative MCP call after every catalog change.
