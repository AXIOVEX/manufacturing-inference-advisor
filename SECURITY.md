# Security

Report vulnerabilities privately to the maintainer rather than attaching controlled data to a public issue. The image runs as UID 10001 without privileges, host mounts or Docker socket. The default gateway blocks network and secrets. Persisted project artifacts are not encrypted; use an approved encrypted volume and access controls before saving confidential designs. The policy engine is a deterministic review aid, not a security control or certification.

